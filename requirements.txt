# Direct runtime dependencies -- what the actual leader/follower/node
# processes and experiments/ scripts import. Transitive dependencies
# (anyio, h11, pydantic_core, etc.) aren't listed here; pip resolves
# those on its own from these packages' own declared requirements.
#
# Dev/test-only tooling (pytest, ruff, mypy, ...) lives in
# requirements-dev.txt instead, which includes this file -- see that
# file for why the split.
aiokafka==0.14.0
fastapi==0.141.1
httpx==0.28.1
pydantic==2.13.4
PyYAML==6.0.3
uvicorn==0.52.3
