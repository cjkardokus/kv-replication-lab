# Replication strategy comparison results

Generated by `experiments/run_comparison.py` on 2026-08-25T19:41:35+00:00. The five leader-follower rows and the `ack_required=0` section below were re-measured and updated on 2026-09-02T23:50:55+00:00 after adding a concurrency-bounding semaphore to `Replicator` (see `leader_follower/leader.py`'s `_MAX_CONCURRENT_REPLICATE_CALLS`); the leaderless rows and sections are unchanged from the original run.

Ranked by staleness rate ascending (lowest/best first). See the top-level `README.md` for what each strategy and parameter means, and `experiments/staleness_load_test.py` / `experiments/leaderless_staleness_load_test.py` for how staleness and failures are measured.

| Rank | Strategy | Config | Staleness rate | Elapsed | Failures | Total requests |
|---|---|---|---|---|---|---|
| 1 | leader-follower | ack_required=4 | 0.00% | 37.2s | 40 | 10000 |
| 2 | leaderless | W=1, R=5 | 0.00% | 56.8s | 11 | 10000 |
| 3 | leaderless | W=5, R=1 | 0.00% | 27.7s | 0 | 10000 |
| 4 | leaderless | W=3, R=3 | 0.00% | 43.1s | 0 | 10000 |
| 5 | leaderless | W=2, R=3 | 0.00% | 43.8s | 0 | 10000 |
| 6 | leaderless | W=1, R=1 | 0.06% | 26.0s | 0 | 10000 |
| 7 | leader-follower | ack_required=3 | 4.40% | 41.6s | 32 | 10000 |
| 8 | leader-follower | ack_required=2 | 12.75% | 45.4s | 34 | 10000 |
| 9 | leader-follower | ack_required=1 | 22.27% | 47.7s | 4 | 10000 |
| 10 | leader-follower | ack_required=0 | 92.81% | 32.7s | 1 | 10000 |

## Why these results look the way they do

### `ack_required=0` has by far the highest staleness of any leader-follower config, but is no longer high-failure or high-latency

This is expected, not a bug -- `ack_required=0` is fire-and-forget: the write path never awaits any follower before returning, so there is zero backpressure on how fast writes can be issued. What that backpressure gap actually does to the numbers above changed with this fix, though:

- **`ack_required=1..4` show a clean monotonic staleness staircase with zero internal exceptions logged on the leader.** Awaiting even one real ack naturally bounds how many replicate calls can be in flight at once (bounded by how many client writes are concurrently in flight). Their nonzero failure counts (4, 34, 32, 40) are ordinary `503`s from a quorum genuinely not being reached within `timeout_seconds` under this benchmark's real, timing-sensitive load -- not exceptions; the leader logs zero of those for any of these four configs. `ack_required=4` (fully synchronous -- every follower must ack before the client sees success) still shows exactly 0.00% -- see below, that's a mathematical guarantee, unaffected by any of this.
- **`ack_required=0` used to have no bound at all on concurrent in-flight replicate fan-out, independent of connection-pool size -- it now does.** A large connection pool (`leader_follower/leader.py`'s `_CLIENT_MAX_CONNECTIONS`) stops replicate calls from silently dropping to `httpx.PoolTimeout`, but on its own never addressed the underlying lack of backpressure: under sustained load the followers (plain uvicorn, no concurrency limiting of their own) got more concurrent replicate traffic than they could drain, which kept each replicate call alive longer, which let yet more unthrottled writes pile up before the old ones finished -- a feedback loop that used to starve the *leader's own* event loop badly enough to time out on its own trivial, in-memory-only client-facing PUT endpoint (2,373 client-visible failures and a 331.4s runtime, ~8.5x every other config, in the previous run of this sweep). `Replicator` now bounds concurrent in-flight replicate calls directly with a semaphore (`_MAX_CONCURRENT_REPLICATE_CALLS`, sized well above what any backpressured `ack_required>=1` config could realistically demand -- see that constant's comment in `leader_follower/leader.py` for the full sizing rationale), independent of the connection pool. The result is visible above: failures collapsed to 1 (down from 2,373) and elapsed time to 32.7s (down from 331.4s, now one of the *faster* configs in the sweep, not the slowest) -- the leader no longer starves. But staleness rose sharply, from 67.63% to 92.81%. That's the backpressure actually showing up where it should: a write that's admitted still returns to the client instantly (fire-and-forget, unchanged), but its replicate calls to followers now queue for a semaphore slot rather than flooding straight past connection-pool limits into an overloaded follower -- under this benchmark's sustained load, that queueing measurably delays when a write actually lands on any given follower, which is exactly what a subsequent read is checking for. `ack_required=0` is now a cleaner demonstration of its own design tradeoff than before: bounded latency and no failure storm, paid for with materially staler reads under load -- rather than the previous run's leader-crash-shaped failure storm standing in for that tradeoff instead of showing it.
- **The connection pool is one global setting, not scoped per `ack_required`, and that remains true independent of the semaphore above.** `Replicator` fans every write out to all 4 followers concurrently regardless of `ack_required`, and never cancels the followers it stops waiting on -- `ack_required` only changes how many of those 4 in-flight calls the write path blocks on before returning. So the *old*, smaller pool (httpx's unset-`Limits` default of 100 connections/20 keepalive, before that fix) was silently dropping excess fan-out to `PoolTimeout` at every `ack_required` value, not just 0 -- just severely enough to matter only at 0's much higher concurrency. Isolation-tested directly: with everything else held fixed (same followers, same load), swapping only the pool setting at `ack_required=2` measured 0.00% staleness / 0 failures under the old pool vs. 8.87% under the enlarged one; at `ack_required=3`, 0.06% (old) vs. 4.60% (new). Enlarging the pool to stop ack_required=0's silent drops therefore also stopped 1..4's silent drops, which is what made the staircase measurable at all in the first place. The staircase is genuine replication lag, not an artifact of the pool fix or of the semaphore fix layered on top of it since -- see the isolation-test evidence above -- but it's worth knowing that connection-pool size is currently one global knob that shapes every config's apparent staleness, not a per-config setting.

### `ack_required=4` shows exactly 0.00% staleness -- provably, not just observed

This is a mathematical floor, not a favorable roll: once `ack_required=4` (= the follower count) is satisfied, *every* follower has already responded to that write's replicate call -- including a follower that rejected it because it already held something LWW-newer, since an "ack" only means "responded", not "applied" (see `Replicator._send`). Either way, every follower's stored timestamp for that key is `>=` the just-acked write's timestamp at that instant, and LWW never regresses a stored timestamp -- so it stays `>=` forever after. A later read of any follower for that key can therefore never see anything older than the newest write that ever reached full `ack_required=4` quorum. This isn't specific to this lab's implementation -- it's the general property that makes `ack_required = len(followers)` "fully synchronous" a meaningful phrase at all.

Earlier runs of this sweep measured a small but nonzero rate here (roughly 1.2-1.3%) despite that guarantee, which was the tell that something *other* than real replication lag was being measured -- tracked down to `staleness_load_test.py`'s write path re-reading the leader's *current* value after a write succeeded (since `PutResponse` didn't echo back the timestamp it stamped), rather than trusting that write's own result. A different, unrelated concurrent write to the same key could land in that gap and get misattributed as this write's own confirmed value, before its *own* replication had necessarily finished. Fixed by having `PutResponse` return the timestamp it stamped directly (see `common/server.py::PutResponse`), so both load tests record each write's ground truth from its own response, never a separate re-read. `ack_required=4` immediately dropped from ~1.2% to exactly 0.00%, with no other change to any replication code -- as clean a confirmation as this kind of artifact gets.

### Leaderless staleness stays near-zero for every config, including `W=2,R=3`, which is genuinely not guaranteed

`W=1,R=5`, `W=5,R=1`, `W=3,R=3` are legitimately guaranteed by the classic W+R>N overlap rule (or one side already equals N) -- 0.00% there isn't a finding. `W=1,R=1` and `W=2,R=3` are **not** guaranteed (`1+1=2` and `2+3=5`, neither `>5`), and as of this fix, genuinely not: `QuorumCoordinator` contacts exactly the peers each config's literal W/R implies (see `replicate_write`/`read` in `leaderless/node.py`, and `tests/test_leaderless.py`'s `test_write_floods_every_peer_regardless_of_w` / `test_read_contacts_exactly_r_minus_one_peers`, which assert the exact contact counts directly) -- no resilience margin quietly padding coverage past the nominal values the way the old `_FANOUT_MARGIN` did.

Both still measure at or near 0.00% run to run (`W=1,R=1` has shown a small nonzero rate on occasion; `W=2,R=3` has not, so far) -- run-to-run noise around a real floor, not two separate coincidences or a residual bug in the fix: **every write floods every peer unconditionally, regardless of W** (durability is fully decoupled from the ack-wait -- see the module docstring), and that flood completes in low single-digit milliseconds on localhost -- far faster than the ~250-330ms average gap between requests touching the same key at this benchmark's throughput (10,000 requests / 100 keys). By the time a subsequent read arrives, the write has almost always finished landing everywhere already, so neither config's real non-guarantee gets much chance to manifest. This was already known and documented for `W=1,R=1` specifically (see `experiments/leaderless_staleness_load_test.py`'s module comment) -- making the fan-out exact rather than margin-padded didn't shrink this effect, it *generalized* it: previously only `W=1`'s unconditional full flood was fast enough to hide behind loopback speed; now every W value floods unconditionally by design, so every non-guaranteed config inherits the same blind spot, not just `W=1,R=1`.

Reproducing genuine leaderless staleness locally now needs the same intervention already scoped for `W=1,R=1`: shrinking the key pool (heavier same-key contention), injecting artificial per-peer replication delay, or real network latency (the planned EC2 stage) -- left undone here since it's a benchmark-realism change, not a coordinator-logic fix.
